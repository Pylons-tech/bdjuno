apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bdjuno.fullname" . }}-test-connection"
  labels:
    {{- include "bdjuno.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bdjuno.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
