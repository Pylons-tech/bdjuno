apiVersion: v1
kind: Service
metadata:
  name: {{ include "bdjuno-backend.fullname" . }}
  labels:
    {{- include "bdjuno-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bdjuno-backend.selectorLabels" . | nindent 4 }}
