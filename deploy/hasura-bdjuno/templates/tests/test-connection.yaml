apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hasura-bdjuno.fullname" . }}-test-connection"
  labels:
    {{- include "hasura-bdjuno.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hasura-bdjuno.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
