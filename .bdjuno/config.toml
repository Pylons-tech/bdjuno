
[cosmos]
  modules = []
  prefix = "pylons"

[database]
  host = "localhost"
  max_idle_connections = 1
  max_open_connections = 1
  name = "bdjuno"
  password = "postgres"
  port = 5432
  schema = "public"
  ssl_mode = ""
  user = "postgres"

[grpc]
  address = "https://grpc.testnet.pylons.tech:9090/"
  insecure = true

[logging]
  format = "text"
  level = "debug"

[parsing]
  fast_sync = false
  genesis_file_path = ""
  listen_new_blocks = true
  parse_genesis = true
  parse_old_blocks = true
  start_height = 1
  workers = 1

[pruning]
  interval = 10
  keep_every = 500
  keep_recent = 100

[rpc]
  address = "http://grpc.testnet.pylons.tech:26657/"
  client_name = "juno"
  max_connections = 20
  insecure = true

[telemetry]
  enabled = false
  port = 500
